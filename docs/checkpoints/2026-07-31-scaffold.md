# Scaffold checkpoint — 2026-07-31

## Outcome

The initial walking-skeleton scaffold is complete. The FastAPI application
exposes `GET /health`, loads environment-based settings without requiring live
credentials, and enforces the first reply-state domain rules with automated
tests.

## Completed work

- Added the FastAPI application factory, typed health route, and public schema.
- Added environment-based configuration with secret fields represented by
  `SecretStr` and safe placeholders in `.env.example`.
- Added the framework-independent reply state machine and a domain guard that
  permits SMTP sending only from `approved`.
- Added the independent `app/`, `tests/`, `docs/`, and `scripts/` structure,
  dependency manifest, Dockerfile, Docker Compose, README, and ignore rules.
- Reserved host ports `8001` for the API and `15433` for PostgreSQL so Project 2
  can coexist with Project 1.

## Verification evidence

- `pytest -q`: 10 tests passed; one upstream Starlette `TestClient` deprecation
  warning was reported by the reused reference environment.
- `ruff check .`: all checks passed.
- `ruff format --check .`: 18 files already formatted.
- `docker compose config --quiet`: passed.
- Secret-pattern scan: no credential-like matches outside `.env.example` and
  private project planning files.

## Risks

- Live Gmail, Telegram, AI, SMTP, and PostgreSQL behavior has not been tested.
- Project 1's virtual environment was reused only as a temporary verification
  tool; Project 2 still needs its own `.venv` for normal local development.
- The current FastAPI/Starlette test stack emits one upstream deprecation
  warning and should be revisited during dependency locking.

## Next smallest step

Design the four first database models and derive their constraints from
duplicate-email and approval-state failure cases before writing SQLAlchemy
models and the first Alembic migration.

## Status synchronization note

This checkpoint should be merged into `PROJECT_STATUS.md`, and the Gmail/state
decisions should be copied into the Project 2 section of
`../PORTFOLIO_CONTEXT.md`, after the current workspace sandbox update error is
resolved.
